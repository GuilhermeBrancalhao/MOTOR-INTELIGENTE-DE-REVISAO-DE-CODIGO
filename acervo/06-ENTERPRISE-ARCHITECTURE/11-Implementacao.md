# Implementação

```python
from engine.volumes import ENTERPRISE-ARCHITECTURE
result = ENTERPRISE-ARCHITECTURE.process(input_data, request_id="uuid")
if result.status == "SUCCESS":
    print(result.payload)
```
