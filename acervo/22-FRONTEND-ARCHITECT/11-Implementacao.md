# Implementação

```python
from engine.volumes import FRONTEND-ARCHITECT
result = FRONTEND-ARCHITECT.process(input_data, request_id="uuid")
if result.status == "SUCCESS":
    print(result.payload)
```
