# Implementação

```python
from engine.volumes import DATABASE-ARCHITECT
result = DATABASE-ARCHITECT.process(input_data, request_id="uuid")
if result.status == "SUCCESS":
    print(result.payload)
```
